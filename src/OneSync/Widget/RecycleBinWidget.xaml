<Window x:Class="OneSync.Widget.RecycleBinWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="320"
        SizeToContent="Height"
        ShowActivated="False">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" ResizeBorderThickness="0" CaptionHeight="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <SolidColorBrush x:Key="PrimaryText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#99FFFFFF"/>
        <SolidColorBrush x:Key="TrackBrush" Color="#33FFFFFF"/>

        <Style x:Key="RestoreBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource TrackBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" ClipToBounds="True">
        <Border.Background>
            <SolidColorBrush x:Name="WindowBg" Color="#CC1A1A1A"/>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Accent bar (blue-grey) -->
            <Border Grid.Column="0" Background="#78909C"/>

            <!-- Content -->
            <StackPanel Grid.Column="1" Margin="12,10,12,10">

                <!-- Header -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding HeaderText}" FontSize="13" FontFamily="Segoe UI"
                               Foreground="{StaticResource PrimaryText}" VerticalAlignment="Center"
                               FontWeight="SemiBold"/>
                    <Button Grid.Column="1" Content="&#x21BB;" Style="{StaticResource IconBtn}"
                            Command="{Binding RefreshCommand}" ToolTip="Refresh" Margin="0,0,4,0"/>
                    <Button Grid.Column="2" Content="&#x2715;" Style="{StaticResource IconBtn}"
                            Command="{Binding CloseCommand}" ToolTip="Close"/>
                </Grid>

                <!-- Empty state -->
                <TextBlock Text="No recently deleted files" FontSize="12" FontFamily="Segoe UI"
                           Foreground="{StaticResource SecondaryText}" Margin="0,12,0,4"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"
                           HorizontalAlignment="Center"/>

                <!-- Loading -->
                <TextBlock Text="Loading..." FontSize="12" FontFamily="Segoe UI"
                           Foreground="{StaticResource SecondaryText}" Margin="0,12,0,4"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                           HorizontalAlignment="Center"/>

                <!-- Deleted items list -->
                <ItemsControl ItemsSource="{Binding Items}" Margin="0,6,0,0" MaxHeight="400">
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Style.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="0,6" Margin="0,2,0,0"
                                    BorderBrush="#11FFFFFF" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Filename -->
                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                               FontSize="12" FontFamily="Segoe UI"
                                               Foreground="{StaticResource PrimaryText}"
                                               TextTrimming="CharacterEllipsis">
                                        <Run Text="{Binding Icon, Mode=OneWay}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding FileName, Mode=OneWay}"/>
                                    </TextBlock>

                                    <!-- Restore / Restoring -->
                                    <Button Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
                                            Content="Restore" Style="{StaticResource RestoreBtn}"
                                            Command="{Binding RestoreCommand}"
                                            VerticalAlignment="Center" Margin="8,0,0,0"
                                            Visibility="{Binding ShowRestore, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
                                               Text="Restoring..." FontSize="11" FontFamily="Segoe UI"
                                               Foreground="{StaticResource SecondaryText}"
                                               VerticalAlignment="Center" Margin="8,0,0,0"
                                               Visibility="{Binding IsRestoring, Converter={StaticResource BoolToVis}}"/>

                                    <!-- Path + metadata -->
                                    <TextBlock Grid.Row="1" Grid.Column="0"
                                               FontSize="11" FontFamily="Segoe UI"
                                               Foreground="{StaticResource SecondaryText}"
                                               TextTrimming="CharacterEllipsis" Margin="0,2,0,0">
                                        <Run Text="{Binding DriveLetter, Mode=OneWay}"/><Run Text=":\"/><Run Text="{Binding FolderPath, Mode=OneWay}"/>
                                        <Run Text=" · "/>
                                        <Run Text="{Binding DeletedAgo, Mode=OneWay}"/>
                                        <Run Text="{Binding SizeText, Mode=OneWay}"/>
                                    </TextBlock>

                                    <!-- Error message -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                               Text="{Binding ErrorMessage}" FontSize="11" FontFamily="Segoe UI"
                                               Foreground="#EF5350" Margin="0,2,0,0"
                                               TextWrapping="Wrap"
                                               Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </Grid>
    </Border>
</Window>
